fun main(args:Array<String>) {

    var id=101  // int
    var name: String= "Khushi"//string  --> when we not mention any data type than we take all value
    //  but if we declare the datatype then not allow  any other type of value
    var salary = 10000.00450//double
    var profit = 50.00//float
    println(id)
    println(name)
    println(salary)
    println(profit)
}